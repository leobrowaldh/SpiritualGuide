{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build API",
      "type": "process",
      "command": "dotnet",
      "args": ["build"],
      "options": {
        "cwd": "${workspaceFolder}/api"
      },
      "problemMatcher": "$msCompile"
    }
  ]
}
